# batch141 SEO 스포크 생성 스펙 (2026-08-20)

작업 디렉토리: `C:\Users\shini\easylaw-scraper`

## 입력
- 배정표: `_b141/args.json` — 40개 슬롯. 각 원소에 idx / domain / perspective / type / tier / case / note / ctaText / ctaLink / linkHrefs.
- 회피 슬러그 전량: `_b141/avoid_slugs.txt` (4,995개)
- 직전 batch140 키워드: `_b141/prev_keywords.txt` (40개)

## 출력
슬롯마다 `_b141/out/out_{idx:02d}.json` 1개. **UTF-8, ensure_ascii=False.**

```json
{
  "slug": "domain-으로-시작하는-영문-케밥-슬러그",
  "keyword": "정리형/구조형 제목 (30~50자)",
  "questionKeyword": "질문형 표현 (20~35자)",
  "ctaKeyword": "짧은 행동 문구 (10~18자)",
  "type": "판단형|절차형",
  "perspective": "victim|accused|neutral",
  "metaTitle": "…| 로앤가이드",
  "metaDescription": "…지금 확인하세요.",
  "intro": "<p>…</p><p>…</p>",
  "sections": [ {"title":"…","content":"<p>…</p><ul><li>…</li></ul>"}, …정확히 4개 ],
  "caseTitle": "실제로 이런 판례가 있었어요",
  "caseSummary": "…",
  "caseTakeaway": "…",
  "faq": [ {"question":"…","answer":"…"}, …5~6개 ]
}
```

`type` / `perspective` 는 args.json 값을 **그대로** 복사한다 (검증기가 대조함).

## 하드 규칙 (하나라도 어기면 검증 FAIL → 재작업)

### 1. 분량·형식
- `intro`: HTML `<p>` 문단 3~4개, **텍스트만 세어 1,200~1,500자** (1,000자 미만 = FAIL). 한글 목표치를 낮게 잡는 경향이 있으니 **밴드 상단 1,400자를 목표**로 써라.
- `sections`: **정확히 4개**. 각 content 450~600자, HTML(`<p>`, `<strong>`, `<ul><li>`) 사용.
  - 섹션 1개는 `<p><strong style="color:#1e3a5f">A. …</strong></p>` 형태의 직답으로 연다.
- `faq`: **5~6개**. answer 각 100~150자.
- `metaTitle`: **30~45자**, 반드시 `| 로앤가이드` 로 끝나고, **아라비아 숫자 1개 이상 포함**.
- `metaDescription`: **80~120자**, 사용자 고민으로 시작해 반드시 `지금 확인하세요.` 로 끝난다.
- `caseSummary` 400~500자, `caseTakeaway` 40~60자.

### 2. 사건번호 (가장 잦은 실패 지점)
- 배정된 사건번호는 **`caseSummary` 안에만** 쓴다.
- `intro`·`sections`·`faq`·`caseTakeaway` 에는 **사건번호를 단 하나도 쓰지 마라.** 본문 혼입 = FAIL.
- `caseSummary` 에 배정된 것 **외의 사건번호를 창작하지 마라.** 다른 번호 등장 = FAIL.
- 사건번호는 args.json 의 `case.num` 을 **한 글자도 바꾸지 말고 원형 그대로** 쓴다 (`97도133`, `86므90` 같은 2자리 연도 구형식도 그대로).
- 서술: `{사건번호}({법원명}, {선고일} 선고) 사건에서 법원은 … 라고 판시했습니다.`

### 3. tier 분기 (args.json 의 `tier`)
- `tier: "supreme"` — 대법원 판례. 단정 서술 가능 ("법원은 …라고 판시했습니다").
- `tier: "lower"` — **하급심**. 반드시 약톤:
  - `caseSummary` 안에 `사례가 있습니다` / `판단한 예가 있` / `달라질 수 있` 중 **최소 1개 표현 포함**.
  - `caseSummary` 앞부분 40자 안에 **"대법원" 단어 금지** (하급심이므로 법원명 그대로 쓰되 대법원으로 격상 금지).
  - "확정된 기준"처럼 쓰지 말고 "이렇게 판단한 사례가 있습니다 — 사안에 따라 달라질 수 있습니다" 톤.

### 4. 관점(perspective) 안전구문
- `accused` 슬롯: **`intro` 안에 반드시 `혐의를 받고 있다면` 또는 `사실과 다르게 신고` 문구를 그대로 포함**한다. 누락 = FAIL.
- `victim` / `neutral`: 상대를 단정하지 않는 서술 유지.

### 5. 금지어 (본문·메타·판례 전 영역)
`범인` / `범죄자` / `유죄입니다` / `처벌받습니다` / `처벌된다` / `100%` / `무조건 돌려받` / `반드시 ~됩니다` 패턴.
또한 `30분 무료 상담`·`변호사 무료 상담` 금지 (기관 무료상담 132·1366 등만 허용).
`⚠️` 를 연달아 두 번 쓰지 마라 (`⚠️⚠️` FAIL).

### 6. 톤 (CLAUDE.md 준수)
- 판단·확정형 금지. "~에 해당합니다" / "받을 수 있습니다" 대신 **"검토해볼 수 있습니다" / "확인해두는 것이 좋습니다" / "정리해두면 도움이 됩니다"**.
- 금액·위자료 단정 금지 → "~수준으로 검토되는 사례가 많습니다".
- 판례 인용문 자체는 단정 가능하나, 독자 사건에 적용할 때는 다시 가능형으로.

### 7. ⚠️ 구법 가드 (2000년 이전 구형식 판례가 19건 배정되어 있음)
사건번호는 원형 유지하되, **본문 법조문은 반드시 현행 기준**으로 쓴다.
- divorce: 호주제(2005 폐지)·간통죄(2015 위헌) **언급 금지**. 현행 민법 제840조 재판상 이혼사유로 서술.
- dui: 현행 혈중알코올농도 **0.03%** 기준 (구 0.05% 금지), 도로교통법 제44조·제148조의2.
- sex-crime: **친고죄 2013년 폐지** — "고소가 있어야 처벌"식 서술 금지.
- assault: 구 폭력행위등처벌에관한법률 조항 상당수가 **형법(제257조 상해·제260조 폭행)으로 이관**됨. 폭처법 조문 번호를 현행처럼 쓰지 말 것.
- traffic: 구 도로교통법 제50조 → **현행 제54조**(사고 시 조치), 미조치 처벌은 제148조.
- jeonse: 계약갱신요구권은 **2020년 신설** — 그 이전 판례에 갱신요구권을 소급 적용해 서술 금지. 대항력·우선변제권(주택임대차보호법 제3조·제3조의2)은 유효.
- 노동(dismissal/wage/retirement): 퇴직금은 현행 **근로자퇴직급여 보장법** 기준 (구 근로기준법 퇴직금 조항 아님). 임금채권 시효 3년.

### 8. 슬러그·중복
- `slug` 는 반드시 **해당 domain 문자열로 시작**하는 영문 케밥케이스.
- `_b141/avoid_slugs.txt` 에 있는 슬러그와 **완전 동일하면 FAIL**. 유사해도 피할 것.
- 특히 `employer-criminal-liability-accident` 와 겹치지 마라.
- 40개 슬롯끼리도 슬러그 중복 금지.
- `keyword` 는 `_b141/prev_keywords.txt`(batch140) 및 다른 슬롯과 **의미 어절 4개 이상 겹치면 FAIL**. 주제를 실질적으로 다르게 잡아라.
- 같은 도메인 슬롯끼리는 **대상자·사건단계·세부유형·증거상태·결과행동 중 최소 2개**가 달라야 한다.

### 8-bis. ⚠️ defamation 민사 판례 (b141 신규 투입)
defamation 4슬롯에 배정된 판례는 이번 배치부터 **민사 손해배상 판례**다 (사건명이 `손해배상(기)` 계열).
- **형사 처벌 각도로 쓰지 마라.** 고소·처벌·전과가 아니라 **불법행위 손해배상 책임 성립 여부·위자료 산정**이 쟁점이다.
- 형법 제307조(명예훼손죄) 대신 **민법 제750조 불법행위, 제751조 위자료** 를 근거로 서술한다.
- 금액 단정 금지 — "위자료가 X만원 수준으로 검토되는 사례가 많습니다" 톤 유지.
- 일부 판례 요지에 `○○지방경찰청`, `노○○`, `甲`, `乙` 같은 **익명화 표기**가 있다. 그대로 베끼지 말고
  "한 지방경찰청", "피해자", "게시글 작성자" 처럼 **자연스러운 우리말로 풀어 서술**하라. `○○`·`甲`·`乙` 이 본문에 그대로 나오면 FAIL.

### 9. 판례-주제 정합성
`caseSummary` 의 판례 쟁점과 `keyword`·`sections` 주제가 **실제로 맞물려야** 한다. 판례를 억지로 끼워 넣지 말고, 배정된 판례의 쟁점에서 출발해 주제를 잡아라. args.json 의 `case.holding` / `case.summary` 원문을 읽고 쟁점을 파악하되, **원문을 복붙하지 말고 재서술**하라.

## 작업 방법
1. `_b141/args.json` 에서 담당 idx 범위만 읽는다.
2. 각 슬롯의 `case.caseName` / `case.holding` / `case.summary` 를 읽고 쟁점을 먼저 정한다.
3. 위 규칙대로 JSON 을 만들어 `_b141/out/out_{idx:02d}.json` 에 UTF-8 로 저장한다.
4. 저장 후 스스로 점검: intro 글자수 / 사건번호 본문 혼입 0 / metaTitle·Description 길이·종결 / sections 4 / faq 5~6 / accused 안전구문 / lower 약톤.

담당 범위 밖 슬롯 파일은 **절대 건드리지 마라** (병렬 작업 중 경합 방지). `spoke-pages.ts` 나 `web/` 아래 파일도 건드리지 마라 — 조립은 메인 루프가 한다.
